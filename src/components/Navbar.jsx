import {AppBar, Toolbar} from "@mui/material";
import React from "react";

const Navbar = () => {
  return (
    <AppBar>
      <Toolbar>Navbar will render here.</Toolbar>
    </AppBar>
  );
};

export default Navbar;
